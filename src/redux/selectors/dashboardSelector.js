/**
 * @method getUserName
 * @summary Action for handle userName reducer
 */
export const getUserName = (state) => state.userName;

/**
 * @method getToken
 * @summary Action for handle token reducer
 */
export const getToken = (state) => state.token;

/**
 * @method getContext
 * @summary Action for handle context reducer
 */
export const getContext = (state) => state.context;
